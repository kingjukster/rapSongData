# Operational scripts

This directory contains experiment launchers, rebuild utilities, and reporting
commands. Reusable application logic belongs under `src/rap_song_data/`;
scripts may remain importable temporarily for compatibility with existing tests
and experiment automation.
